import React from 'react';

class PasswordChange extends React.Component {
    // a constructor to construct the PasswordChange Component
    constructor(props) {
        super(props);
        this.state = {
            oldPassword: '',
            newPassword: '',
        };
    }
    // add all the features
    render() {
        return(
            <div>
                //some elements
            </div>
        )
    }
}

export default PasswordChange;